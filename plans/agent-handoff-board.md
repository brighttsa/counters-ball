# Agent Handoff Board

Shared task board for Claude Code and Codex. Read at the start of every session.
Claim files before editing; move tasks and leave a note when you stop.
Protocol: see `AGENTS.md` → Collaboration protocol.
Live ownership: run `node scripts/collaboration-workflow.mjs status` in your
own clone. Each owner's live `plans/claims/<owner>.json` and dirty files take
precedence over stale copies of this board. Until both clones adopt the helper,
coordinate board claims manually. See `docs/collaboration-workflow.md`.

## In progress
| Task | Owner | Branch | Files / folders claimed | Started |
|---|---|---|---|---|

## Blocked
(Public deployment below is resolved; kept for history.)

### Public deployment - 2026-09-17
- **2026-09-24: LIVE at `bee3a95`** (KONK! rebrand, Street Legends, chalk notes, physics feel, soundtrack). Owner said "push live". All 168 runtime files byte-identical to local; plans/tests/AGENTS.md 404; live page boots with no console errors and asks for the Home track. **Redeploy gotcha:** since the repo gained a GitHub remote (private `brighttsa/counters-ball`), `npx vercel deploy --prod` from the repo root sends git metadata and Vercel Hobby **BLOCKS** it (commit author `BSKT <bskt@…local>` is not the Vercel owner; CLI just hangs on "Building…", `vercel ls` shows UNKNOWN, API `readyState` BLOCKED). Deploy from a git-free copy instead: copy `index.html assets src styles .vercelignore` plus `.vercel/project.json` into a temp folder and run `npx vercel deploy --prod --yes` there. Two blocked deployments (`8jg6w0z6l`, `6xhy99mry`) are harmless leftovers.
- **2026-09-23: LIVE.** `vercel deploy --prod` from `76d0c69` (CLI signed in by the owner via device login; folder linked through a git-ignored `.vercel/project.json` to `prj_Z0z3Jvo960l7DTZP1bYjLk6Z3dZa`). Deployment `dpl_9BTimEi8oFTAuJjH4emUczrJwGvA`, aliased to https://counters-ball-3d.vercel.app (public, no SSO). All 142 runtime files byte-identical to local; `.vercelignore` keeps plans/docs/tests/AGENTS.md off the site (404). Challenge URLs answer 200. Not yet checked in a real browser on the public URL (in-app browser was refused the site) — open it once on a phone. Redeploy: `npx vercel deploy --prod` from the repo root.
- 2026-09-23 update: both alias URLs (`counters-ball-3d.vercel.app`, `counters-ball-3d-brighttsa-gmailcoms-projects.vercel.app`) now answer HTTP 200 unauthenticated — the SSO wall is gone — but they serve the 2026-09-17 build (no Counters Ball headline, no share/challenge markup). Redeploy blocked in this session: no Vercel CLI login and no Vercel connector; owner offered the connector. Deploy only runtime files (`index.html`, `assets/`, `src/`, `styles/`).
- User subsequently said Yes to reconnecting/enabling public access. Main task is checking once; success is not yet confirmed.
- User authorized a NEW PUBLIC DEPLOYMENT. Vercel created `dpl_8DuNJxffSAwaR1jr8juAGp8YgQkJ` from `e982a1b` (48 runtime files only), but the unauthenticated alias returned HTTP 302 to Vercel SSO: NOT publicly accessible.
- `get_deployment` returned HTTP 403 scope authorization for `team_aBn8fiH2WyGceG4B0GFLAWzi`; `list_teams` was empty. Pending action: enable public access using the proper Vercel account/project, then verify unauthenticated availability. Main deployment task owns connector verification.
- Supplied review passed 43 JavaScript syntax checks, 79 local references, 28 bare imports, and whitespace checks; no static blocker found. Mobile browser verification remains unverified/admin policy blocked. Historical completed phase plans are unchanged.
- Full URLs and evidence: [deployment handoff](reports/pm-260917-1214-deployment-handoff.md). Reflection: [journal](../docs/journals/260917-1214-deployment-access-blocker.md).

## Up next (unclaimed — pick one, move it to In progress)
| Task | Suggested owner | Notes |
|---|---|---|
| Check a challenge link preview | owner | KONK! is live (2026-09-24). Send a challenge link in WhatsApp and check the `og:image` preview. |
| Share card corner art | either | `results-share-card-canvas.js` still paints the old red cap print (`paintInkCapPoster`) faintly in a corner; owner removed it from the home screen as competing with the KONK! cap. Ask whether to drop it or use the KONK! cap/paper ball. |
| KONK! brand re-exports | either | Art source is `plans/visuals/konk-brand-identity.html`; `node plans/visuals/konk-brand/build-konk-brand-assets.mjs` (needs Google Chrome) re-exports SVG/PNG and the game copies in `assets/`. Don't hand-edit `assets/konk-logo.svg`. |
| Measure frame rate on a phone-sized viewport with the pane visible; tune shadow map size / bokeh if under 50 fps | either | 4096² sun shadow + BokehPass are the likely costs |
| Physical-phone pass (iOS Safari + Android Chrome) | user + either | Landscape-first, portrait available. Verify intended cap selection (28px halo, 6px separation), aiming comfort, shot outcomes, camera modes, replay skip, audio, sustained fps/heat. Reduce ambiguity band if clear intended touches are refused; enlarge if wrong selections persist. |
| Human playtest of Street Legends Acts 2–3 (draw rate) | user | Current rules already include golden flick and +2 flicks. Use `plans/reports/street-legends-flow-recovery.md` to test comprehension, retry intent and genuine human draws before any balance change. |

## Done (newest first)
| Task | Owner | Commit | Handoff note |
|---|---|---|---|
| Cohesive mobile landscape and closer table framing | Codex | Commit containing this note | UI/UX + SCAMPER pass focused on phone landscape cohesion: Home now uses a stable two-column poster/action composition with stronger safe-area gutters, larger logo/action hierarchy, utilities kept with the action stack, and hidden tiny star text in short landscape. Match HUD landscape widths now scale proportionally instead of hard-coding a cramped strip, with short-height objective clipping to protect the table. Broadcast/Tactical/Free mobile-landscape framing reserves less empty HUD space so the table sits closer while still fitting rails/goals. Verified focused mobile/camera group: 31 pass; all `src` syntax clean; full suite now returns to the known 255 pass / 2 pre-existing star-copy failures (`konk-copy-contracts`, `menu-and-results-copy`). Opened `tests/phone-mockup-home-check.html` on local 4181 for visual review. |
| Broadcast match start camera | Codex | Commit containing this note | Match entry and hot-seat handover now refuse to start in Street Level or Free even if that was the saved camera preference; saved Tactical still starts Tactical, everything else opens Broadcast. Street/Free remain selectable once play is active, so the player chooses the dramatic/inspection views instead of being dropped into them at kickoff. Targeted camera/input tests pass (21), source syntax clean. Full suite still has the 2 pre-existing star-copy failures from the Unicode-icon change (`konk-copy-contracts`, `menu-and-results-copy`). No browser visual pass needed for this logic-only camera preference guard. |
| KONK branded first load | Codex | Commit containing this note | Replaced the brown first paint with an immediate ink KONK boot slate ("Setting the caps..."), removed the brown `html`/canvas background, and fades the loader after the app has started the Home/attract render loop. Source syntax checks pass and local 4181 preview requested the updated HTML/CSS/JS/logo cleanly. Full suite currently has 2 pre-existing failures on latest `main` from the Unicode-star-to-SVG change (`konk-copy-contracts`, `menu-and-results-copy`); this task did not touch those files. |
| Phone Home notch and landscape fit | Codex | Commit containing this note | Home now reserves safe-area gutters for phone portrait/landscape, keeps the phone action stack compact without vertical scrolling, and gives landscape phones a dedicated two-column composition instead of a squeezed portrait stack. Added/updated `tests/phone-mockup-home-check.html` + JS to render iPhone SE/14/14 Pro Max and Samsung S20/S23 Ultra portrait and landscape shells, with a saved verification board at `plans/reports/phone-mockup-home-check/phone-mockup-home-check.png`. JS syntax clean; full suite 255 pass. No deployment. |
| Cross-phone home mockup fit | Codex | Commit containing this note | Broadened the Home phone treatment beyond iPhone 14 Pro Max: removed the old short-landscape utility-button pinning, scoped the phone-landscape layout to common phone widths up to 932×480, added clamp-based notch gutters, and added a tighter 568×320/620×360 fallback. This keeps Settings/Credits with the Home action stack across small SE-style, standard, Plus/Pro Max and Android-ish landscape sizes rather than parked near the simulated browser chrome. JS syntax clean and 255 tests pass. Chrome simulator matrix could not be completed in-tool because the extension reported the user/browser unavailable mid-run; needs owner visual spot-check across the simulator device list. No deployment. |
| Mobile simulator home fit | Codex | Commit containing this note | Focused the Home screen on the phone mockup experience: narrow phone frames get a tighter poster stack, and short landscape phone frames reserve a notch-safe left gutter while keeping Settings/Credits in the action stack instead of pinned near the simulated Safari bar. Checked the active Chrome mobile simulator mockup and direct 800×360 / 360×800 phone-sized views on port 4181; simulator screenshot compositing intermittently paints the iframe black, but DOM remains live and console stays clean. 255 tests pass; JS syntax clean. No deployment. |
| Home settings, credits and information hierarchy | Codex | Commit containing this note | Home now gives one dominant featured Play action, grouped match modes, and a separate Settings/Credits row. Settings reuses the pause controls without pausing an attract match; Credits opens with Bright Tsa's childhood love of Counters ball and names Suno for music, Codex/Claude for the build. Removed the redundant Home tagline, restored logo size on short portrait phones, and kept Credits/Back visible at 568×320. Browser checked 320×568, 390×844, 635×863, 568×320 with clean console. 255 tests pass; no deployment. |
| Align adaptive home actions | Codex | Commit containing this note | At the owner's 859×863 portrait preview, the hero's 420px menu container began at x43 but its buttons were only 300px wide and centered at x103. Explicit one-column grid sizing now aligns the primary and secondary actions with the heading at x43. Reloaded the live localhost:4181 tab and confirmed matching bounds and a nonblank screenshot; no gameplay changes or deployment. |
| Adaptive hero, compact menus, and mobile settings sections | Codex | Commit containing this note | SCAMPER pass kept the pitch primary: medium portrait hero moves into a bottom poster composition with a larger logo; portrait attract poster aims toward play; 568×320 home no longer clips the logo; compact match intro clears the sound button; short-height event callouts shrink. On phones, pause Settings switches Sound/Camera/Table directly rather than nesting a long scroll; desktop and tablet retain the full settings card. Browser checked 568×320 home, acts, intro, HUD and each settings section (no inner scroll); 320px portrait and 768×1024 hero/settings were checked, console errors empty. 254 tests pass, including section-selection regression. Physical iOS/Android, larger text, and sustained fps still need a device pass. No deployment. |
| Mobile settings and camera selector fit | Codex | Commit containing this note | Pause settings now fit a short landscape phone instead of clipping camera choices and Done; narrow portrait keeps Done visible while settings scroll, and portrait/tablet camera choices use two readable rows. Short-height camera selector shows all four modes plus Reset without hidden scrolling. Browser checked 320×568 portrait, 844×390 landscape, and 768×1024 tablet portrait on port 4181; Tactical selection changed the view and console errors were empty. 253 tests pass; CSS diff check clean. Physical iOS Safari/Android Chrome touch and performance checks remain open. No deployment. |
| Pause menu: four actions + settings on the card back (SCAMPER brainstorm → two-face card) | Claude Code | `699e4dc` | Replaced the 10-button flat pause menu with a card that flips between two faces. Front: Resume, Restart match, How to play · Kwame's Corner, Quit to Pitches, and a ⚙ Settings link. Back: three groups (Sound/Camera/Table) with chalk chips showing all options (aria-pressed for the active one). Choosing a music level or switching effects on lifts the ♪ all-sound mute. Escape from the settings face returns to the front first, second Escape resumes. New `src/ui/pause-card-faces-and-setting-chips.js` (23 lines: `markChoice`, `showPauseFace`, `pauseFace`). CSS: `.pause-face` flip animation 220ms, `.chip` with slanted clip-path (yellow on active), 44px min touch targets; landscape phones layout Sound and Camera groups side by side. Removed old `cycle-camera-view`, `toggle-effects`, per-button `::after` content rules. 253 tests pass (including new chip interaction tests). Browser: front face verified at desktop width; settings back face and mobile width not yet browser-verified. konk.world domain live. |
| Soundtrack from the owner's three recordings (owner brief "KONK! — integrate my three original soundtrack files"; mapping approved: Home = Three-Contact Motif, Classic/2-Player/Kwame = Found Object Groove, Legends = Bottle Cap Challenge; exception, 128 kbps copies and freeze-on-pause approved) | Claude Code | Commit containing this note | Assets: `assets/audio/konk-home-theme-three-contact-motif.mp3`, `konk-classic-match-found-object-groove.mp3`, `konk-street-legends-bottle-cap-challenge.mp3` (ffmpeg libmp3lame 128k, art and Suno comment stripped; 8.0 MB total; originals in ~/Downloads untouched, md5 recorded in the session). Code: `src/audio/soundtrack-track-list-and-screen-routing.js` (`SOUNDTRACK` with loop points, `musicForScreen`), `soundtrack-loop-seam.js` (`bakeLoopSeam`, 150 ms equal-power), `soundtrack-music-director.js` (idempotent `request`, 1.2 s crossfade, stale-decode token, 32 kHz OfflineAudioContext decode, LRU of 2, results dip 0.63, `duckForGoal` 0.5 for 2.5 s, bus straight to destination at 0.32 × volume), `music-and-effects-audio-settings.js` (`MUSIC_LEVELS`, `applyAudioSettings`), `soundtrack-app-wiring.js` (`MenuScreens.onShow` → request; keydown unlock; visibilitychange → `sound.setHidden`). Sound board: `setEffectsOff`, `setHidden`/`applySuspension`, `unlock()` attaches `sound.music`. Pause menu: `#music-volume-toggle` (cycle-music-volume), `#effects-toggle` (toggle-effects) replace the Sound row; both lift ♪. Save: `musicVolume` ∈ {0, .35, .7, 1}, `effectsOff`. Loop search: offline onset/level correlation (scratch script), loop lengths repeatable for Home/Classic; Legends least certain. Tests: `tests/soundtrack-music-direction.test.mjs` (7). 252 pass. Browser (silent test tab, sink none): unlock gating; Home→levels→intro same voice; kick-off → classic, one live track; turns keep the voice; pause freezes ctx time; resume same voice; goal dip 0.5 → back; results 0.63; title → home; Legends; results; Play again → intro → match same voice; quit → home; Music High/Off/Low + Effects Off saved and restored after reload. Owner's shared save: test-only fields reverted. Not yet: listening checks (loop seams, mix level) by the owner; production (needs "push live"). |
| Lorry chalk note clear of kickoff caps (owner: "Fix the lorry note under the kickoff caps") | Claude Code | Commit containing this note | `placeClear` in `chalk-note-clear-placement.js`: x slides first; if none clear, retries the x slides with z nudges ±0.04/0.08/0.12 (`NUDGE_Z` exported). `PITCH_X` 1.45 → 1.47 so the x limit (1.2) admits the 1.2 anchors of the ruler and lorry notes. Clearance sweep (full formation on both sides + props, 8 turns per venue): no note covered; lorry outer stops sit at z ±0.64. New test walks the lorries through 8 turns against the full kickoff formation (fails with the nudges removed). 245 pass. Browser (tab-5): Harmattan act 2, live away note at (1.20, −0.64) clear of Abdul's corner cap; home outer stop placed at (−1.20, 0.64); console clean; owner save restored byte-identical. |
| Chalk tallies upright in Tactical (owner: "Fix the chalk tallies in Tactical view too") | Claude Code | Commit containing this note | New exported `screenUprightYaw(camera)` in `chalk-table-score-and-flick-tallies.js` (yaw from the camera's world up, column 1 of matrixWorld; null when level) replaces the position-based yaw for the score and tallies (touchline clamp unchanged, now fed the new yaw) and is reused by `venue-rule-chalk-notes.js`. Test: overhead landscape/portrait/turned and broadcast cameras all put the chalk's top up the screen. 244 pass. Browser (tab-5): Schoolyard Tactical at 917×751 and 375×812, tallies/score/notes upright; console clean; owner save restored byte-identical. |
| Venue rules chalked on the table (owner: "Chalk the venue rules on the table") | Claude Code | Commit containing this note | Each venue state has `chalkNotes()` → `[{ x, z, text, side? }]` (ruler `NEXT: <angle>` at (±1.2, .33); dish `GAP NEXT` per next gap >10°, reach DISH_RADIUS+.15; clay one `NO STRAIGHT GOALS` at (0, .22); toll `SHUTS NEXT` on `LANES[closedLane(p,1)]` at plaza.x∓.4; lorry `LORRY NEXT` at (±1.2, center(g,1)); coin `STRIKE · n/3 LIT` beside `nextStack` tinted by side, else `GOAL OPEN`). Mechanic definitions expose it; wiring creates `createVenueRuleChalkNotes` (new `src/scene/venue-rule-chalk-notes.js`, reuses newly exported `chalkSurface`/`chalkify`/`chalkTint` from the chalk scoreboard) when a stage and document exist, and on each turn calls `set(notes, tableObstacles(physics))`; new pure `src/scene/chalk-note-clear-placement.js` slides a note along x only to the nearest spot clear of bodies and segment samples. Notes turn by the camera's up vector (upright in Tactical too; the chalk tallies still turn by position and read sideways in Tactical, left as is). HUD `setObjective(goal, reading)` shows the goal, reading in a `.visually-hidden` span. Ruler ghost line solid (0.022 wide, 0.7). Tests: new `tests/venue-rule-chalk-notes.test.mjs` (4). 243 pass. Browser (tab-5, 917×751): all six venues' act 2 in Broadcast, Schoolyard also Tactical and Street; console clean; owner save restored byte-identical. Known: at a lorry's outer stops the note can sit under a home cap in the full kickoff formation. |
| Match HUD one frame + between-match polish (brainstorm recommendation, owner: "Apply recommendation") | Claude Code | Commit containing this note | New `styles/match-hud-one-frame-and-aim-fade.css` (linked last): scoreboard/flick-meter/`#hud-objective` stack as one strip (objective top +88 desktop, +72 portrait phone; yellow edge moves to the objective where it hangs below; landscape phones keep the objective beside); `.turn-banner` visually hidden (still aria-live); `body[data-screen=match] .sound-btn` hidden; camera toolbar joined, `.player-camera` moved to the corner, panel width fixed 280; aim fade (`body:has(#game-canvas.aiming)`) on pause button + camera tray. `MatchHud.setTurn` writes a 1.8 s `.turn-call` into `#flick-meter-label`. Pause menu `#sound-pause-toggle` (toggle-sound; `setSoundIcon` syncs `data-value`). Camera button markup `.camera-menu-label` (small caption + view name). Presentation director skips MATCH POINT when `goalsToWin` is 1. Results: Next primary only on a solo win with a next table, otherwise Play again primary (menu focuses it); confetti cleared before a solo loss card. Act card: removed `#circuit-venue-conditions` / `#circuit-venue-rules`; opponent line `vs Kid · Difficulty · terms`; stars as glyphs with aria-label; `fillVenuePreview` lost its `conditions` arg. Owner questions left unanswered, defaults taken: Tactical peek stays its own button; layout checked in both phone orientations. Tests: 2 new + venue-panel test updated. 239 pass. Browser (tab-5; desktop 917×751, 375×812, 812×375): act card, one frame with turn call and revert, aim fade opacities, camera panel open above the tray, pause Sound on/off, loss card Play again primary+focused with no confetti; console clean; owner save restored byte-identical. |
| Physics feel pass: rail bite, clean contact, quick settle (owner: "Now do the physics feel pass") | Claude Code | Commit containing this note | New `src/gameplay/flick-feel-contact-rail-and-settle-rules.js`: `contactRestitution` (0.6 glancing → 0.8 square, by squared cosine of the closing velocity to the normal; replaces fixed 0.72), `RAIL_RESTITUTION` 0.72 (was 0.55) + `RAIL_GRIP` 0.8 on the along-rail component in `bounceAxis`, `settleFriction` (+0.6 × frictionScale below 0.12 u/s). Feedback strength factors now derive from these. Static segments (rulers) untouched. Setting the constants to 0.72/0.72/0.55/1/0 restores the old engine exactly. Baseline found two bugs: mirror-aimed banks (what the chalk bank hint draws) scored 0/336, and the Kwame bank lesson had no makeable aim from its cap; now 116/336 (all that reach the line) and ≈6° at ¾–full power. Settle tail 0.34 → 0.11 s mean; turn 1.68 → 1.48 s. AI one-flick check (60 seeds, kickoff + scattered): medium goals 4 → 6, hard 9 → 8, territory unchanged. Tests: new `tests/flick-feel-physics-regression.test.mjs` (5; all fail on the old constants). 237 pass. Browser: live practice table, bank lesson layout, straight full flick banked in off the rail under the rail-only rule; console clean; owner save restored byte-identical. |
| First-launch Kwame, camera cues, pause settings (brainstorm recommendation, owner: "Apply recommendation") | Claude Code | Commit containing this note | Title: `.home-learn` (play-practice) + `.home-just-play` (skip-practice → `progress.practiceSkipped`) replace `.home-featured` while neither `practiceDone` nor `practiceSkipped`; the small Practice button hides meanwhile (`MenuScreens.setFirstLaunch`, called from `showTitle`). Camera: new `src/ui/camera-view-icons-and-copy.js` (label, purpose, key, inline SVG icon per view; `viewName`), used by the panel (one view per row, kbd badges and a keys note only under hover+fine pointer), the Camera button (icon + label + C) and `announce` toast ("Now: X · key", 1.8 s) fired from `PlayerCameraController.select` when the player chose it. Pause: `cycle-camera-view`, `reset-hints` (`JustInTimeChalkHints.reset`), `how-to-play` (two-press confirm mid-match, then Kwame's Corner). Tests: 3 new. 232 pass. Browser (fronted separate tab, 877×751): title first-launch state with practice flag cleared, then normal state; Just play saved the flag and started the featured act; panel icons/purposes/keys legible after moving to one column (two columns clipped the Broadcast key); toast; pause menu labels and view cycling; console clean; owner save restored byte-identical. Owner had finished Kwame's Corner in their own tab, so their title shows the normal button. |
| Kwame's Corner practice table (owner: "Build Kwame's Corner practice table") | Claude Code | Commit containing this note | New: `src/levels/kwame-corner-practice-table.js` (Classic Schoolyard, `practice: true`, keeper only, `awayFlickLimit 0`, 99 flicks/goals), `src/gameplay/kwame-corner-practice-lesson-steps.js` (6 lessons: pick, flick, ring, bank, peek, street; `applyPracticeSetup`; bank lesson uses `goalRequiresTouchOf ['rail']`; `PRACTICE_MAX_MISSES` 3), `src/ui/kwame-corner-practice-coach.js` (card `#kwame-coach` under the scoreboard, SVG ring/bank path, retry reset, kickoff re-setup, sign-off then `finishPractice`). Wiring: `trackFor('practice')`, always unlocked; title button `play-practice`; `showLevels('practice')` → title; intro "Kwame's Corner / Practice", no star lines, back/quit labelled Home; `progress.practiceDone`; camera fit keeps clear of `#kwame-coach`; just-in-time hints skip practice; `.chalk-hint-glow` now generic. Tuning from live play: the owner missed the first bank layout 4 times (≈2.3-unit path, ±6° slack), so the ball now starts near the rail (≈1.6 units, ±9°) and three misses move on. 229 pass (4 new). Browser (fronted separate tab, 1048×751): owner played lessons 1–4 for real (pick/flick completed by play, ring and bank retries reset the table); lessons 5–6 via peek and the 3 key; sign-off → title, coach and glows removed, practiceDone saved; Quit to Home mid-practice clean; console clean; owner save restored byte-identical. main.js is now 221 lines (over the 200 guideline). |
| Just-in-time chalk hints (brainstorm recommendation, owner: "Apply recommendation") | Claude Code | Commit containing this note | New `src/gameplay/chalk-hint-shot-geometry.js` (pure: `pathClear` against circles and enabled segments, posts ignored; `findRailBank` mirror-the-goal one-rail bank, null when the straight line is open; `hiddenFromCamera` sphere-occlusion ray) and `src/ui/just-in-time-chalk-hints.js` (SVG polyline + chalk-font label anchored by projection each frame; `#camera-peek` glow). Driven from a new `onFrame` hook in `startGameRenderLoop` after the camera update. Shows only for a human side's own aiming turn, never in attract/preview/replay/pause/first-shot tutorial; one hint at a time, 9 s max, cleared on flick; seen ids in localStorage `counters-ball-chalk-hints-v1` (once ever; no reset setting yet). Bank line is a solid stroke (owner dislikes dashed lines). Tests: 5 geometry tests. 225 pass. Browser (fronted separate tab, Classic Schoolyard 751×751): with a cap placed on the ball-goal line the bank hint drew ball→far rail→goal with its label and stored "bank"; peek glow + label above the button; street label; hint cleared on the owner's own flick (owner was playing in the test tab). Real triggers for street/peek were exercised through the same module but not provoked by live play; owner save restored byte-identical. |
| One-piece aim arrow (owner: "I don't like the dash arrows" — confirmed: the chalk aim line and the aim arrow looking banded) | Claude Code | Commit containing this note | Chalk dashed line: never committed; owner's open tab had loaded it mid-experiment (still running it, mid-match) — a refresh clears it. Arrow: removed notch, core, enamel edge and ring shadow from `flick-vector-meshes.js` / `aim-trajectory-power-ring-and-rim-glow.js`; band starts at the ring's outer edge (1.7r, no gap); ring and band share one tint lerped ivory→gold by 0.75×power; glow ring now hover-only; contact ghost tint ivory→gold by alignment. Readability after first capture looked faint on pale concrete: ring opacity .92, outline .34 at 1.2× width. 220 pass. Browser (fronted separate tab, Schoolyard Broadcast, 80% power): 751×751 and 844×390 show one continuous translucent band with a thin outline and a gold ring; owner save restored byte-identical. |
| Lighter, translucent aim arrow (owner: "didn't mean totally change the arrow… felt heavy and less premium. It should have a bit of transparency") | Claude Code | Commit containing this note | Kept the ribbon arrow. `flickWidthPixels`: touch 24+28p → 14+12p (full power 52 → 26px), mouse 18+24p → 11+10p (42 → 21px). Opacities in `flick-vector-meshes.js`: ribbon .95 → .72, ribbon shadow .5 → .26, ring .9 → .7 (thinner 1.25–1.7), ring shadow .45 → .2, glow .2 → .14, notch .95 → .8, gold core .85 → .6 (fill ×.6), enamel edge .8 → .5, ghost .7 → .55; shadow/edge scale 1.2/1.12 → 1.12/1.1. Regression test floor updated (14/11px minimum, 26/21px maximum). A chalk-line alternative was built first and discarded before commit on the owner's clarification (patch kept in the session scratchpad only). Test Three r160 in /tmp had been wiped; re-fetched from jsDelivr `three@0.160.0/+esm` (REVISION 160). 220 pass. Browser (fronted separate tab): 751×751 and 844×390 Schoolyard Broadcast aim at 80% power: slim, table chalk visible through the band, caps uncovered; owner save restored byte-identical. |
| Broadcast shows both goals (owner: "zoomed in too much for broadcast. Can't even see both poles") | Claude Code | Commit containing this note | Replaced the ball-following Broadcast (`broadcastFrame`/`followBallPose` and the controller's per-frame follow, all removed) with `broadcastPose`: fits both goal frames (x ±1.5, or the lorries ±1.9 on lorry acts; y 0–.26; z ±.3) plus the table's full width across the middle, centred between the measured HUD top and a 60px bottom reserve. Tries rises 1.45/1.8/2.2/2.7 per 2.8 back (~27°/33°/38°/44°) and takes the first where the width covers ≥80% of the height between the HUD bars, else the best. Measured: 847×751 → 44°, 1280×720 → 27°, 1920×1080 → 33°, 844×390 → 27°; portrait unchanged (goals top/bottom); both goals in view at all sizes. Test rewritten (13 sizes × 2 mechanics: both goals and table width clear of the HUD, tight fill). 220 pass. Browser (fronted separate tab, 1048×751): Schoolyard both goals at the edges, all caps in view; Harmattan both lorries in frame; console clean; owner save restored byte-identical. |
| Fade props near the camera (owner: "Make the props fade when close to the camera") | Claude Code | Commit containing this note | New `src/fx/near-camera-prop-dither-fade.js`, owned by `PlayerCameraOcclusion` (so it runs only while player control is active; `restore()` switches it off for replays/goal cams). Props = opaque meshes on the table envelope with top between .08 and 1.2 (above ball .07, caps, rails .05). Per-pixel screen-door dither (4×4 Bayer discard) by view distance: solid beyond 0.8× camera-to-aim distance, 25% at 0.5× and nearer; no alpha sorting, shadows kept. Per-pixel because toll booths are merged per material (`static-mesh-merge-by-material`): one near booth fades, the rest stay solid. Each prop draws with a cached fading copy of its material (shared materials stay solid on the table/pieces); re-applied each frame because coin stacks swap to a lit material. Unlit materials untouched. Tests: 4 new (selection, band, shader patch, material swap). 220 pass. Browser (fronted separate tab, Street): Veranda's nearest pot faded to a ghost, far pots solid; Roadside booths (~0.9× distance, next to the action) stay solid by design; Lights Out renders, no console errors; owner save restored byte-identical. Not checked: phone GPUs (discard cost is small), Broadcast lorry ends by eye. |
| Street Level venue sweep (owner: "Check the Street Legends venues at street level") | Claude Code | Commit containing this note | All six Act 1 tables at 1048×751 in a fronted separate tab. Schoolyard, Kiosk, Lights Out: fine. Veranda (clay pot) and Roadside (toll booth) show a large prop in the low foreground; raycasts from the camera to every home cap found none hidden, so left as part of the low view. Harmattan: the goal lorry sat under `#hud-objective` (3 lines, bottom 148px vs fixed 136px reserve). Fixes: Street frames the lorry's side boards and cab (x ±1.785, y ≤ .26, goal z ±.3) unclamped on lorry acts; `viewBounds` takes the larger of the fixed reserve and the measured bottom of `.scoreboard`/`#hud-objective` + 8px (affects all views; no DOM in tests → unchanged there). Harmattan Street now 3.21 units (lorry roof 154px, objective ends 148px); Roadside far goal clear. New test: lorry in view and clear of the reserve at 4 sizes × 3 stops. 216 pass; console clean; owner save restored byte-identical. Not checked: landscape-phone/portrait Street on each venue; objective text changes mid-match are picked up at the next reframe, not instantly. |
| Closer Street Level (owner: "Zoom more into street level. It looks far") | Claude Code | Commit containing this note | Street fitted the near-end corners (±1.05 at the viewer's goal line), cap, ball and far goal with a 2.8 minimum distance, so it framed nearly the whole table (4.3–4.9 units; 7.6 with the cap near the far goal). Now it fits the cap rim, the ball and the goal mouth clamped to `STREET_LOOK_AHEAD` (1 unit) past the ball, inside the HUD bounds (scoreboard reserve, 60px bottom), minimum 1.2, lower angle (landscape y .55 ≈ 27°, portrait .8). Open-play distance 4.46→1.85 at 1048×751; own goal out of frame except when the cap is at its own goal line. This deliberately drops Codex's earlier near-edge framing; the owner asked for closer. Test rewritten: cap, ball and the path halfway to goal clear of the HUD, goal on screen, cap rim < 22% of the short screen side, and distance < 2.2 in open play at 1280×720 (old 4.3). 215 pass. Browser (separate fronted tab, 1048×751, Classic Schoolyard): close low shot behind the caps, far goal at the top, no console errors, owner save restored byte-identical. Not checked: every venue's tall props (booms, pots, lorries) at this low angle; physical phone. |
| Square camera views (owner: "Tactical table looks slanted") | Claude Code | Commit containing this note | Tactical looked down along (.01, 1, .09): with a near-vertical view the tiny x component sets the roll, turning the table ~6.4° (37px lean of the centre line at 847×751). Broadcast (.15 x) and the overview/Free reset (.3 x) had the same class of yaw. All now have zero sideways component (landscape x = 0, portrait z = 0); Street's three-quarter angle unchanged. New test measures the centre line's on-screen lean for Tactical/Broadcast/Free at 6 sizes (<0.5px; old code fails at 37.4px). 215 pass. Browser (separate tab, 847×751): Tactical halfway line vertical, rails parallel to screen edges; owner save restored byte-identical. |
| Calm camera at turn changes (owner: "camera angle change when it's a player's turn is overwhelming") | Claude Code | Commit containing this note | Cause: owner's saved home camera is Street; `playerCameraPose('street')` framed from behind `rules.turn`, so against the computer the view swung to the AI's end each AI turn and back on the owner's turn, reframing at the 16/s snap rate (~0.25 s). Now `playerCameraPose(..., viewer)` takes the view's side (controller passes `owner()`: the human vs AI, the side to play in 2-Player); Street reframes only when `rules.turn === owner`. `cameraTransitionBlend` takes a rate: `CAMERA_SNAP_RATE` 16 for player-initiated moves (select, peek), `CAMERA_GLIDE_RATE` 3 (~1 s) for automatic ones (Street's new-turn reframe, 2-Player hand-over, returning from intro/replay/goal camera); snaps back to 16 once the glide arrives so Broadcast's ball follow stays responsive. 214 tests pass (2 new: glide timing, Street stays behind the viewer on the opponent's turn). Browser (fronted separate tab, Classic Schoolyard vs AI, Street): camera moved 0 during the AI's aim and shot, then glided 0.76 over ~1 s (max 0.073/frame) at the owner's turn, staying at the home end; console clean; owner save restored byte-identical. Not checked: replay-return glide by eye, 2-Player hand-over by eye, physical phone. |
| FC-style Broadcast camera (owner: "still haven't solved prioritizing pitch over secondary environment… reference EA SPORTS FC26"; "pitch" = the table) | Claude Code | Commit containing this note | `player-camera-view-poses.js`: `broadcastFrame` fits a thin slice across the table's full width (outer rails ±1.2) between the scoreboard reserve and a 60px bottom reserve (bottom chips sit in the corners), landscape ~27° side-on, portrait (-1.4, 3.8) behind-goal; measures how far the view reaches along the table and returns the pan range so the view stops at the rail ends (±1.8, lorry acts ±2.2). `followBallPose` slides the aim point to the ball's x (centres the table when the view is longer than it). Controller eases the follow (rate 5/s) each frame, never while an aim is held (`aimLocked`). Tactical unchanged; Free orbits at `overviewPose` distance. Tried and dropped: a fixed length window with across-pitch following, which cropped the rails on squarer screens and mis-centred under perspective. Tests: whole-table checks now cover Tactical/Free; new Broadcast test (13 sizes × 2 mechanics) asserts rails fill the space between HUD bars, follow is monotonic, both goals reachable; fails on the previous code. 212 pass. Browser (separate tab, 1280×720, 847×751, 844×390, 375×812): table fills frame, room only a thin band; a real keyboard flick panned the camera to the away goal and stopped at the end; console clean; owner save restored byte-identical. Owner's saved home camera is Free, so their match will not show Broadcast until they pick it. Not checked: physical phone, all venues at the new zoom. |
| Closer match camera (owner: "pitch looks so far… main focus should be the pitch not the environment") | Claude Code | Commit containing this note | `player-camera-view-poses.js`: Broadcast/Tactical fit `pitchFramePoints(session)` (batten outer edge ±1.72×±1.2, batten top, matchstick crossbars; lorry acts ±2.08) on every viewport; the old desktop path fitted the whole table sheet (±2.25×±1.45) with wide margins. `centredPitchPose` evens the space above/below the pitch inside the HUD reserves (`hudReservePixels`, same 136/144 and 80/72 px as before), horizontal bound .90→.95, Broadcast elevation ~43°→~35°. Measured rails width: 1078×751 65%→84%, 1440×900 58%→85%, 1920×1080 53%→85%, 844×390 44%→72%, 390×844 73%→82%. Street and Free unchanged (Free zoom range follows the new Broadcast distance). Tests now use the shared frame points; new test asserts rails fill >80% width at 1078–2560 px. 212 pass. Browser (separate tab, 1078×751, 844×390, 375×812): pitch fills the view, HUD clear, console clean. Owner's open tab needs a refresh to pick it up. Follow-up venue sweep: all 6 Classic tables (via 2-Player, which unlocks them) and all 6 Street Legends Act 1 tables at 1078×751, Legends tables at 844×390 and 375×812 portrait. Pitch, goals, lorries, toll booms, pots, coin stacks and dish in frame and clear of the scoreboard/callout and bottom chips; console clean; owner save restored byte-identical. Scenery only intrudes as intended (Lights Out spotlight beams rise behind the scoreboard). Not checked: physical phone. |
| Remaining UX audit actions (+ review/merge of Codex `69382fa`) | Claude Code | Commit containing this note | **Merge:** reviewed the full `3db9a0a..69382fa` range (9 commits, 41 files: first-shot guidance, UI motion, camera framing/presets, Street Legends retry cues, KONK copy deck), ran all 200 tests on the exact commit in a scratch worktree, fast-forwarded `main`; syntax check of 136 source files, 4180 match smoke test clean. **Chalk font error** (`chalk-table-score-and-flick-tallies.js:174`): reproduced in a test as `TypeError: Cannot read properties of undefined (reading 'then')` when `document.fonts.load` returns nothing; now `Promise.resolve(document.fonts?.load?.(…)).then(redraw).catch(…)`, so neither a missing promise nor a failing late redraw can throw or leave an unhandled rejection (test fails on the old line, passes now). **Restart/Quit confirmation:** once any flick has been played in a live (not attract, not ended) match, the first press arms the button ("Sure? Press again to restart/quit", 4 s) and only a second press acts; reopening pause disarms. Escape now opens pause through the same `actions.pause` route. **Conditions copy:** every Street Legends act said "No fixed obstacles"; `tableConditionCopy(level)` now leads with the table's moving feature (e.g. "Turning ruler in play", "Toll booms open and close · 1 fixed obstacle"); plural fixed ("1 fixed obstacle"). Codex's `conditionObstacleCopy` contract kept. **Keyboard shot:** `src/gameplay/keyboard-flick-aim.js`, owned by `HumanDragAimInput` and driving its selection/pull/visuals: any aim key picks up the cap nearest the ball aimed at it; ←/→ (A/D) turn 5° (Shift 1°), ↑/↓ (W/S) power 10–100%, Q/E switch caps, Space/Enter flick, Esc puts the cap down (next Esc pauses). Same velocity as a drag of that length, still-drag gesture (no boost). A key guide shows while keyboard-aiming; the tutorial tip adds "Or press Space to aim with the keyboard" on fine-pointer devices only. Camera keys 1/2/3/C untouched; Space/Enter on a focused button keep their meaning. 211 tests pass (11 new). Browser (4180, fresh profile, cleared after): real keydown events on the page played a keyboard flick (home flicks 0→1), Esc put down then paused, restart confirm armed then restarted, conditions lines correct, console clean. Not checked: screen-reader announcement of the armed label; physical keyboards beyond Chrome. |
| Implement approved KONK! copy deck | Codex | Commit containing this note | Applied the approved home, mode, venue, act, HUD, result, challenge and share language. Shared UI/results and match-reaction strings live in small copy modules; all six Classic blurbs and 18 Street Legends act cards retain their real mechanics. Two factual adjustments to the deck: Classic Veranda says pebbles (not the Legends pots), and Esi's Classic blurb does not imply a one-shot match. Goal reactions rotate per scoring side only on real goals. 200 tests and source syntax checks pass; browser on a fresh 4181 origin checked home, venue list, intro and live match at phone/landscape widths with no overflow or console errors. Results layout uses existing scrollable card but was not visually exercised in browser; physical-device review remains. No deployment. |
| KONK! copy audit and approval deck | Codex | Commit containing this note | Audited home, modes, 18 Street Legends acts, six Classic blurbs, match/controls, results, rivalry, challenge/share, fallback, and the act-intro/mechanic cue inventory. Proposed voice guide, current-to-proposed deck, event variants and implementation guardrails in `plans/reports/konk-in-game-copy-audit-and-deck.md`. No runtime strings changed; owner approval required before implementation. No deployment. |
| Street Legends flow recovery cues | Codex | Commit containing this note | Six venue-specific NEXT FLICK cues appear once after the first non-scoring solo Act 1 shot settles; Act 2 loss/draw results offer a matching next-try decision. No rule, AI, star or tiebreak changes. 195 tests pass. Real Schoolyard flick at 844x390 and 390x844 showed cue, 10→9 flicks, 0-0 score, no phone HUD overlap. Human draw/comprehension playtest remains; see `plans/reports/street-legends-flow-recovery.md`. No deployment. |
| Fix visible camera preset selection | Codex | Commit containing this note | Reproduced owner's desktop/mobile failure by clicking visible preset text: the menu's focusout handler closed before its radio activated. Removed that premature dismissal; outside pointer, Escape and Close still work. After module refresh, visible labels selected all four modes at 844px and Tactical at 390px. Direct-input browser checks had masked this bug. Full suite passes; current preview tab needs reload for new module. No deployment. See `plans/reports/mobile-camera-framing.md`. |
| Make preset camera switching immediate | Codex | Commit containing this note | Live 4181 match confirmed Tactical, Broadcast and Street are distinct, but preview throttling made eased switches appear stalled: render `dt` caps at 50 ms even when frames arrive ~1 second apart. Camera blend now uses real elapsed time only; simulation and flick state unchanged. Fresh Schoolyard preview showed Tactical on the next captured frame. 193 tests pass. Existing tab must refresh for new modules; physical-device response unverified. See `plans/reports/mobile-camera-framing.md`. No deployment. |
| Make camera modes usable in live matches | Codex | Commit containing this note | Live Schoolyard inspection found Street Level clipped the near pitch. Reframed against the near edge, active cap, ball and far goal; 844x390 and 390x844 captures now keep the playable area visible. Tactical, Broadcast, Street and Free respond in the harness. Near-edge projection regression added; 192 tests pass. Known refresh-time chalk font error and unattributed MutationObserver diagnostic remain; physical device check pending. No deployment. See `plans/reports/mobile-camera-framing.md`. |
| Closer mobile pitch and reliable match cameras | Codex | Commit containing this note | Broadcast portrait pitch fit is steeper and fitted with bounded search, maintaining tested playable rails, both goals and HUD margins at 320/375/390/430px and tablet/landscape sizes. Street Level tracks the new resting arrangement and ignores the other side's stale selected cap. Player controls yield to replay and return afterward. Existing input cancellation leaves flicks at zero; focused camera/aim tests and the full 191-test suite pass. Harness earlier showed Street/Free changes and tablet aim cancellation without body movement; a final visual capture after margin correction was blocked by the in-app browser connection. See `plans/reports/mobile-camera-framing.md`. Physical device feel/FPS and replay visual check remain; no deployment. |
| Responsive and interruptible UI motion | Codex | Commit containing this note | Pause opens instantly; other screen entrances use one 200ms strong ease-out instead of nested 450ms fade/300ms slide. Callout replacements retain current opacity/offset, remove clip-path animation, and reduced motion retains opacity fades without travel. Tutorial uses direct transforms and caches dimensions, invalidating on resize/content size changes with ResizeObserver. 189 tests pass, including replacement continuity/priority and reduced-motion fade tests; syntax/diff checks pass. Browser on 4181: Schoolyard guidance rendered, pause computed animation is none. Existing MutationObserver diagnostic remains; physical-device FPS and frame-by-frame polish unverified. No physics changes or deployment. Peer main remains 3db9a0a; includes previous 6baff90 UX pass. |
| First-shot guidance and readable match information | Codex | Commit containing this note | Based on exact Claude main 3db9a0a. First human flick guidance now works outside tutorial venues, remembers completion separately from progress with guarded storage, ignores attract play and rejected flicks. Selecting/cancelling no longer completes the lesson; hot-seat guidance targets the current side. Chalk artwork retained alongside readable screen-space score/flick counts; objective no longer occupies their slot. Full 186-test suite passed, then all 3 guidance tests passed after adding an integration case; source syntax and diff checks passed. Browser Roadside at 390x844 and 844x390: counters, objective and guidance visible; drag-rotation cancellation kept score 0-0 and 8 flicks. Existing MutationObserver diagnostic plus refresh-time font-loading error recorded (see Up next); no console-clean claim. No deployment. |
| Apply KONK! branding | Claude Code | Commit containing this note | Owner chose the name **KONK!** (brand exploration: `plans/visuals/konk-brand-identity.html`, source of truth for the art; `node plans/visuals/konk-brand/build-konk-brand-assets.mjs` re-exports SVG/PNG into `plans/visuals/konk-brand/` and the game copies into `assets/`: `konk-logo.svg`, `konk-app-icon-180.png`, `konk-share-preview.jpg`; `konk-favicon.svg` is hand-simplified for 16px). Home: `<h1 class="home-logo"><img>` replaces the COUNTERS BALL headline and the hidden `title-logo` fetch (`MenuScreens.loadLogo` removed); tagline *Tiny pitch. Big moments.*; "Street Legends · featured mode" now labels the featured act instead of sitting above the title. Also: `<title>`, meta description, Open Graph/Twitter tags (absolute og:image on counters-ball-3d.vercel.app), apple-touch-icon, orientation prompt kicker, share-card kicker `KONK! · FULL TIME`, share text "… in KONK! Beat that:", PNG name `konk-result.png`. **Deliberately unchanged:** localStorage keys (`counters-ball-3d/…`, renaming would wipe saves), `window.__countersBall`, repo/folder/Vercel project names. Verified 184 tests; browser at 1280×760, 390×844, 844×390 (landscape logo shrunk to 250px so the ball's path isn't clipped); console clean after a cache-busting reload (a stale cached module had thrown from the removed loader — Vercel revalidates statics, so players won't see that mix). Not yet deployed. Follow-up (owner: "the ball must look like folded paper"): the logo ball is now the game's paper wad — nine hand-placed fold planes with sun-side tones, ink creases, and faint exercise-book ruling clipped per plane so the lines break at every fold; the icon's chalk rim follows the paper outline; favicon ball redrawn to match. Then removed the home screen's big red cap print (`#home-cap-print`, owner: "another bottle cap … is interrupting"); `paintInkCapPoster` stays for the share card corner. |
| Split `main.js` | Claude Code | Commit containing this note | 263 → 203 lines (about 30 of them imports); the app flow (title → levels → intro → match → results) stays in `main.js`. Moved out: `src/levels/level-tracks-and-challenge-unlocks.js` (`trackFor`, `challengeForLevel`, `isTrackLevelUnlocked`, `takeChallengeFromUrl(loc, hist)`), `src/ui/full-time-results-presentation.js` (`presentFullTimeResults`: stars or series/head-to-head, verdict line, share prep, card fill), `src/ui/menu-button-action-routes.js` (`createMenuActions`: every `[data-action]` route plus the sound/scoreboard toggles; built after `MenuScreens`, which reads `actions` lazily). Behaviour unchanged, except one fix found while verifying: the 2-Player "GAME n" kick-off callout was priority 3, so on first-to-1 tables (Schoolyard) MATCH POINT (4) always hid it; now 5. 184 tests pass (3 new: challenge unlock scope, URL read/strip, every `data-action` in `index.html` has a route). Browser: challenge link → intro → verdict → levels clears it; 2-Player Schoolyard kick-off shows GAME 1; console clean; owner's save restored byte-identical and tab reloaded. |
| Rivalry, share cards, challenge links (multiplayer audit "do now" items) | Claude Code | Commit containing this note | All client-side; no backend, no accounts. **2-Player Table:** intro shows two name fields under the team colours (prefilled from `progress.versusNames`); names flow to HUD (`MatchHud.setNames`), turn banner, goal callout (`options.playerNames`, a live object `HotSeatRivalry.names` filled at kick-off), results title/opponent line. First-to-two-wins series in memory (`src/core/hot-seat-series-and-rivalry-record.js`): draws don't count, kick-off alternates (`MatchSession.start(side)`), "GAME n" callout at kick-off, "<NAME>'S FLICK / Pass it over" callout (priority 2) whenever the turn changes hands. Results in versus: Rematch is primary and skips the intro (`prepareMatch(i, { rematch: true })`), becomes "New series" once decided. Head-to-head per pair saved as `progress.rivalries` (key order- and case-free; sanitised on load, omitted when empty so old saves keep their exact shape). **Share:** results card gets Share; card PNG (1080², `src/ui/results-share-card-canvas.js`) is pre-rendered when results appear so `navigator.share` runs inside the tap's activation (Safari). Fallback without Web Share: download PNG + copy message. **Challenge links:** AI matches share `?beat=<id>&m=<campaign|legends>&s=h-a&f=n` (`src/core/challenge-link-codec-and-comparison.js`, strict validation, no names — the chat shows the sender). Boot reads and strips it (`history.replaceState`), shows a challenge card, the named table opens even if locked (stars earned there are recorded normally), intro lists "Your friend's mark", results show beat / matched / short (win > draw > loss, then goal difference, then fewer flicks). Honour system by design. 181 tests pass (10 new). Browser (background tab, owner's save backed up and restored byte-identical, tab reloaded after): challenge link → card → portrait prompt → intro with mark line → verdict; share payload with 192 KB PNG; card render checked visually; versus Ama vs Kofi through a 3-game series (callouts, alternating kick-off, lines, Rematch/New series, share text); intro and results fit 844×390 without scrolling; console clean. **Open:** real-phone check of the share sheet — does WhatsApp keep the message text when an image is attached (iOS may drop it; then the link needs a second send); links only reach friends once public deployment works (LAN URL for now); `main.js` is 263 lines, above the 200 guideline (composition root; candidate split: level tracks/unlock/challenge lookup). |
| Golden-flick tiebreak with +2-flick fallback | Claude Code | Commit containing this note | Answers the board's open "golden-flick sudden death vs +2 flicks" question for Street Legends Acts 2–3 draws: both, staged. `MatchRules` (`match-rules-turns-goals-and-results.js`): new `grantTiebreak()` called from `passTurnTo` when both sides run dry level — 1st time grants 1 flick each as `tiebreak: 'golden'` (any goal ends the match immediately, even below `goalsToWin` — `finishGoalCelebration` checks `tiebreak === 'golden'` first), 2nd time grants 2 flicks each as `'extra'` (normal `goalsToWin` rule, just a bigger budget), 3rd time is a real draw. Flicks are granted via a new `tiebreakBonus {home,away}` added on top of `flickLimitFor()` — **not** by decrementing `flicksUsed` (that was my first attempt; it silently caps at the original `flickLimit` since `flicksUsed` floors at 0, so a "+2" grant after a golden-flick round gave 1 flick, not 2 — caught by a test, not by eye). `flicksUsed` itself is never rearmed, so the `threeStarFlicks` efficiency star stays honest through a tiebreak win. Solo challenges (`awayFlickLimit: 0`, e.g. Schoolyard's ruler-bank challenge) are explicitly excluded — the away side was never meant to flick back, so they still end in a flat draw exactly as before. New `rules.on('tiebreak', stage)` event in `match-session-feedback-hooks.js` reuses the existing single-lane HUD callout (priority 6, between MATCH POINT's 4 and GOAL's 10) and the existing `matchPoint` sound cue — "GOLDEN FLICK / One flick each. Next goal wins." then "TWO MORE EACH / Still level. Two more flicks each." No new UI, no new sound, no per-venue changes: every venue's own goal mechanic (padlock/dish/maze/lorry/ruler/toll) is untouched, since a tiebreak only changes turn/win-condition bookkeeping. 171 tests pass (6 new in `match-rules-regression.test.mjs`, one rewritten since the old "both exhausted = instant draw" behavior is deliberately gone). Browser-verified end to end in the owner's own idle tab (was on the level list, not mid-match): drained a real Schoolyard 2-player match to 0-0 exhaustion → GOLDEN FLICK banner + flick meter showed 1/1 → drained again → TWO MORE EACH + 2/2 → drained again → real draw, `flicksUsed` 15/15 (12+1+2), stars 0. Separately, on Roadside (`goalsToWin: 3`, not 1): forced 1-1 in regulation, drained to golden flick, scored one sudden-death goal → match ended 2-1 to the scorer, two goals short of the normal win threshold, real results screen showed "Accra Reds win! 2 — 1" — confirms the golden-flick shortcut isn't an artifact of a `goalsToWin: 1` level. Owner's save was swapped for a throwaway before both rounds of testing and byte-for-byte restored after each (see the CSS-consolidation row above for why that discipline matters on this origin). Open: which stage numbers (1 golden flick, then +2) actually feel right is still a human-playtest question — the mechanism is built and proven correct, the constants aren't validated against real play yet. |
| CSS consolidation: type scale, spacing grid, hover-gating, dead-rule sweep | Claude Code | Commit containing this note | Follow-up to the UI audit row below. Four layered stylesheets (`broadcast-identity-and-circuit`, `broadcast-match-presentation`, `ink-impact-global`, `ink-impact-match`) had already been collapsed into one rule per element; this pass: (1) one 10-token colour palette replacing 66 raw values across three overlapping sets (2 yellows → 1, 3 near-whites → paper/paper-muted) — `game-ui-base-and-hud.css` `:root`; (2) a 10-step type scale (`--text-xs`…`--text-hero`) replacing 33 arbitrary font-size values, applied everywhere via search/replace; (3) every padding/margin/gap snapped to the 4px grid; (4) `:hover` rules gated behind `@media (hover: hover)` so a tap on touch no longer leaves a button stuck lit (`.btn`, `.screen-title .btn`, `.level-card`, `.player-camera button`); (5) swept for orphaned classes/keyframes after the rewrite — none found. Also fixed while verifying: locked level cards now hide their empty star row (showed 3 dim stars + LOCKED, redundant); short-screen level cards drop the place line to keep two-line act names readable; pause/results cards on landscape phones (max-height:500px) go side-by-side instead of scrolling (menu-stack becomes a 2-col grid, results-stars flow in a row) — both previously overflowed their card at 844×390. Chalk-white in the 3D scoreboard (`chalk-table-score-and-flick-tallies.js`) now matches `--paper` exactly (`#f5f1df`, was `#f4efdf`). 167 tests pass, no CSS-only regressions in a before/after computed-style diff across three sizes (844×390, 390×844, 1280×760) — the only unchanged-but-surviving issue is a pre-existing 2px sub-pixel overflow on the in-match scoreboard at portrait width (357×46 content vs 44px box), present before this refactor too, never chased down. Verification harness used a background browser tab; test states that force campaign/legends match results run against a *throwaway* progress object swapped into `localStorage` and restored after — necessary because this origin's `localStorage` is shared across tabs, so a naive test would silently write fake stars into the owner's real save (this happened once earlier in the session on Veranda Act 1 and was manually reverted; the harness above is what should be reused instead of ad-hoc `s.rules.end()` calls on a real session going forward). Owner's real save confirmed byte-identical before and after this session's checks. Open: the 2px scoreboard rounding; nobody has looked at the result on a physical phone yet. |
| UI audit (alignment, colour, a11y, motion, layout, type, spacing, states, shadow) + defect fixes | Claude Code | Commit containing this note | Measured with an in-page audit (text size, WCAG contrast over resolved backgrounds, target size, focus, names) on title/levels/intro/match/pause/results at 844×390 and 390×844. **Fixed:** keyboard focus ring was invisible on every slanted `.btn` (clip-path clips outlines) → focused buttons drop the slant and show a 3px ring; level-strip labels were 10–11px → 12px; level Back button 40px tall on landscape phones → 44px; added pressed feedback (`:active` brightness + 1px) since the yellow primary had none on touch; `--font-display` was Impact, which phones don't ship → Anton webfont (near-identical); three Arial rules → `--font-body`. After: no text <12px, no targets <44px, no text contrast failures (unearned stars 4.24:1 are glyphs, ≥3:1), no unnamed controls, no horizontal scroll, console clean. Reduced motion already covered globally. **Not changed (need owner call):** 66 raw hex values across 3 overlapping token sets (two yellows, three near-whites); 33 font sizes in CSS; spacing off the 4/8 grid in places; sticky white `:hover` after taps on touch; old cardboard shadow/animation rules now dead under the ink layer. |
| Intro waits for Kick Off | Claude Code | Commit containing this note | Owner: players never got to read the rules. `prepareMatch` scheduled an automatic kick-off 3 s after the intro opened; removed, so the rules card stays until Kick Off. Found while checking: `MenuScreens.show` focused the first match of a combined selector list in document order, so the intro focused "Pitches" (Enter left the match) and results focused Pitches too; now tries primary → level card → any visible button in priority order. 167 tests pass. Browser: intro still up with the match waiting after 6 s (owner's tab, on title); background tab: focus lands on Play (title), selected act (levels), Kick Off (intro; activating starts play), Pitches (2-player results, no Next). Intro still hides the star goals (`ink-impact-match.css`); they only show on results. |
| Procedural sound design pass | Claude Code | Commit containing this note | Still zero audio files. (A sample library the owner offered was a leaked copy of a commercial pack; nothing from it was used.) Every cue is now a struck table object, respecting `66db37c`'s "no imitated local music or voices": new `src/audio/table-object-sound-recipes.js` (bottle tap, coin ring, table knock, paper crinkle, cardboard tap, net catch). Skill cues, match point and win lost their triangle beeps; stars are struck bottles; menu tick is a cardboard tap; flick is nail snap + cap thump + scrape (no pitch sweep); paper ball crinkles on firm hits; goal adds the ball settling in the net under the whistle; unused `goalCheer`/`groan` removed. Voices accept an optional stereo pan (`StereoPanner`, only added when panned); contacts, wall knocks and the net pan by screen position (`src/audio/screen-space-stereo-pan.js`, ±0.6, follows the screen so portrait works). Noise voices start at a random buffer offset so repeated hits don't share one grain. 167 tests pass (5 new). Browser: OfflineAudioContext renders of every cue — no NaNs, no clipping; peaks: whistle −14.6, win −14, match point −14.8, street play −17, clink −18, counter −19.6, flick −20, sweet spot −20.8, net −23.8 dB; panned clink ±0.81 L/R energy balance. Open: nobody has listened yet — needs an ears-on pass on phone speakers and headphones. |
| Extract full-time results card | Claude Code | Commit containing this note | Results moved out of the HUD into `src/ui/ui-full-time-results-card.js` (`FullTimeResultsCard.fill/cancelReveal`, pure `fullTimeNote`); `main.js` owns the card and passes `HOME_TEAM.hudColor` (was read back from the HUD's CSS variable). HUD renamed `ui-match-hud-pause-and-results.js` → `ui-match-hud-scoreboard-callouts-and-tutorial.js` (it held neither pause nor, now, results): 135 lines, results 81. Also fixed a second Roadside-only line: solo-challenge "out of flicks" said "Watch the amber lane" at every venue → "Read the table and set the ball up for it." 162 tests pass (solo-challenge note test added). Browser (background tab, 2-player): real `onEnd` → results shows title, chalk "1 — 3" with home #d6503a / away #4f86c6, Next hidden, save unchanged, console clean. Owner's save: fake Veranda Act 1 stars removed at owner's request (Act 2 shows locked with its 3 stars until Act 1 is won). `main.js` now 212 lines. |
| Polish menus and results | Claude Code | Commit containing this note | **Level select** (`styles/circuit-venue-preview.css` rewritten): one-viewport layout at every size; venue copy on a solid ink panel (was bare text over the 3D table) with Enter inside it; all acts in one sideways strip with vertical venue labels, snap, wheel→sideways, selected act scrolled into view; landscape phones hide place/conditions/blurb. Old duplicate level-card rules removed from `ink-impact-home.css`/`ink-impact-global.css`. Opponent/rules lines use ` · ` and capitalised difficulty. **All menus:** secondary buttons get a fill (were ink-on-ink text); back buttons say Acts in Street Legends, Pitches elsewhere (`data-track-label`); pause toggles both read "Label · Value". **Results:** fixed "The booms bow to you" appearing on every non-Roadside Legends win → "<rival> gives you the table. Next act unlocked."; full-time score chalked in team tints (Cabin Sketch). 161 tests pass (3 new, DOM-stubbed). Browser in a separate tab (owner was playing in the main one): level select at 844×390 / 390×844 / 1280×760 with no page or panel scroll; pause and 2-player results at 844×390 (results fit after short-screen score size); console clean; progress unchanged by the checks. Note: an earlier forced-results check in this session wrote 3 stars for Veranda Act 1 into the owner's save before they played Acts 2–3; left as is, owner told. Open: `ui-match-hud-pause-and-results.js` now exactly 200 lines (results could move to its own module); turn banner squeezes to 48px on landscape viewports narrower than ~500px (older `mobile-tablet-match-layout.css` rule, not a real phone size). |
| Chalk scoreboard default + objective gap | Claude Code | Commit containing this note | Chalk is now the default scoreboard; only an explicit `hudStyle: 'broadcast'` is saved/restored (Pause toggle still switches). In chalk mode the Street Legends objective takes the scoreboard's old top-left slot in every layout (`body.hud-chalk .legend-objective`, max-width leaves room for pause). 158 tests pass. Browser (4180, no saved style → chalk): objective at 12,12 clear of pause at 844×390 (right edge 606 vs pause 784), 390×844 (318 vs 330, wraps to 3 lines) and 1280×760; screen scoreboard hidden; console clean apart from the known stale-module entry. |
| Home featured act, Counters Ball headline, chalk scoreboard prototype, heat meter cut | Claude Code | Commit containing this note | **Why Roadside:** `9b3c147` hard-coded the Roadside slice as the home feature and attract table; never generalised. **Home:** headline now COUNTERS BALL (kicker STREET LEGENDS / GHANA). Featured act = `pickFeaturedLegendAct` (`src/levels/featured-home-legends-act.js`): the next act after a won act at the same venue, else the last act played (`progress.lastLegendAct`, saved on every Legends match), else Roadside Act 1 (Act 2 once won). Kicker, venue, act title, button ("Play Jamestown" etc.) and attract table all follow it. **Heat meter:** removed (markup, `hud.setHeat`, all CSS); skill heat still drives the ambience audio. **Chalk scoreboard (prototype, off by default):** Pause → "Scoreboard: Broadcast/Chalk", saved as `progress.hudStyle`. `src/scene/chalk-table-score-and-flick-tallies.js` chalks the score at halfway and remaining flicks as tally gates on the near touchline (Cabin Sketch, team colours tinted toward chalk); each panel faces the camera and stays inside the touchline; hysteresis stops flicker near the pitch axis. Chalk mode hides the screen scoreboard and flick meter only. 158 tests pass (8 new). Browser (4180): default home, Kiosk Act 2 after a faked Kiosk Act 1 win (restored after), Roadside Act 2 with owner's real save; chalk at 1280×760, portrait pane and landscape; goal redraws "1 — 0"; tallies tick down; toggle hides/shows both ways and persists; console clean apart from one stale-module error from the first cached load. Open: phone-size readability on a real device; far-side vs near-side preference; decide whether chalk becomes default; objective line keeps its old top offset in chalk mode; `src/main.js` now 209 lines. |
| Remove full-time loss groan | Claude Code | Commit containing this note | `match-session-feedback-hooks.js`: full time plays the `win` tone only when the human (or either side in versus) wins; losses and draws are silent apart from the whistle. 150 tests pass. Browser (4180, fresh reload): forced AI win emitted no sound events and no groan; home win emitted `win`; console clean. `goalCheer`/`groan` methods remain on the sound board but nothing in match flow calls them now. |
| Restrained goal celebration (emotional canvas: "earned neighbourhood pride", intimacy over spectacle) | Claude Code | Commit containing this note | `match-session-feedback-hooks.js` only. Goals no longer play the synthesized crowd cheer/groan; whistle + neighbourhood startle + paper confetti carry the moment. Goal camera trauma 0.5→0.28; bloom 0.55→0.18 on plain goals, 0.38 on labelled (skill/venue) goals. Plain player-goal line "What a flick!"→"Yours."; versus "X score!"→"X."; AI line unchanged. Slow-mo, confetti, replay, full-time win/loss tones unchanged. 150 tests pass. Browser (4180, Schoolyard Act 1): goal fired whistle only, trauma 0.28, bloom 0.18, banner "GOAL / Yours. / 1-0 / Adabraka Primary, Accra", console clean. Open: hear it on a phone; decide whether the full-time `loss` groan should also go. |
| Integrate Codex mobile/ink/camera commits (9b3c147..f1ff113) | Claude Code | `f1ff113` (fast-forward) | Read all 8 commits. Syntax clean, all files <200 lines, 150/150 tests pass. Scope: ink art direction across all venues; zero-size viewport guard (fixes the GL_INVALID_FRAMEBUFFER_OPERATION item); player-controlled cameras (Tactical/Broadcast/Street/Free); landscape-first mobile with portrait fallback; match camera usability; Schoolyard shot memory; tighter mobile pitch framing with HUD reservations; ambiguous-touch rejection. No deployment. |
| Pitch-first mobile framing and touch clarity | Codex | `f1ff113` | Mobile/tablet Broadcast and Tactical fit playable rails/goals rather than decorative apron, with HUD reservations and extra Harmattan lorry clearance. Short-landscape Broadcast is lower; desktop framing unchanged. Touch near-ties refuse selection with feedback, clear selection retains ring/vector plus accessible status. Objective/tutorial disappear only during aim; score/turn/flicks and Tactical peek remain. 150 tests pass, syntax clean; eight final viewport HUD/pixel checks (320/375/390/430 portrait, 568x320, 844x390, 768x1024, 1024x768), six-venue render checks before final conservative goal-height adjustment, rotation cancellation zero shots and Classic hot-seat actual input release verified. Existing unattributed MutationObserver diagnostic persists. Starting touch values: 28px halo, 6px nearest-distance separation; physical phone/tablet test still required: intended cap should select without wrong-cap shots; reduce ambiguity band if clear intended touches are refused, enlarge only if wrong selections persist. Physics/strength unchanged. No deployment. |
| Remember Schoolyard winning ruler banks | Codex | Commit containing this note | A human-vs-AI Schoolyard ruler-bank goal reaching the winning score is remembered only on confirmed home match end. Separate guarded local storage, session fallback, idempotent completion; no physics/stars/unlock changes. Schoolyard Legends intros recall the achievement across acts, never Classic/versus/other venues. Existing goal visual/audio feedback retained. 147 tests pass including six memory tests (negative cases, blocked storage, fresh-module restore); syntax checked. Live earned-shot-to-return browser playtest remains outstanding; no deployment. |
| Landscape-first mobile and tablet play | Codex | Commit containing this note | Owner chose landscape-first, portrait optional. Pre-match rotation prompt with Back/portrait opt-out, no live turn behind it; compact portrait and short-landscape HUD, pause/turn separation, smaller-screen Heat hidden, safe-area controls and scrollable camera selector. 141 tests pass; eight viewport HUD/pixel checks, portrait/rotation entry paths, tablet drag-rotation cancellation (zero flicks), Classic hot-seat real input release. See `plans/reports/mobile-tablet-landscape-play.md` for evidence and physical-device gaps. No physics changes or deployment. |
| Match camera usability and touch clarity | Codex | Commit containing this note | UI/UX Pro Max pass: anchored selector leaves toolbar stationary; 48px targets, 8px gaps, hover/pressed states; Close, Escape-before-pause, focus restoration, outside/focus dismissal; presets auto-close while Free controls stay open. Notices clear on next outside pointer interaction or screen change. No physics changes. 137 tests and syntax checks pass. Browser verified Escape without pause, preset auto-close, Free panel, Close/reset; nonblank canvas checks at 320/375/390/430/1280 on 4181; mobile/desktop screenshots in visualization folder (`ux-camera-320.png`, `ux-camera-desktop.png`). Existing unattributed MutationObserver diagnostic persists. Physical touch and short-landscape visual check remain unverified. No deployment. |
| Player-controlled match cameras | Codex | Commit containing this note | Tactical/Broadcast/Street/Free, selector, 1/2/3/C, hold Tactical peek, bounded OrbitControls pad/zoom, reset and local per-side preferences. Camera commands cancel a held drag without firing; no physics/rules edits. 137 tests pass. Same-position Roadside captures, unchanged bodies/zero flicks on camera switch, four-mode Classic phone-width pixel checks. Physical touch, full hot-seat handover and goal/replay/restart stress still need checks; see `docs/player-camera-system.md`. No online mode exists, no deployment. |
| Whole-game Ink and Impact art direction | Codex | Commit containing this note | Shared ink renderer across all six venues, 18 acts, Classic and 2-player: bounded contours, cap silhouettes, light bands, shadow dots and impact strokes. Venue lighting/material hues preserved. Shared HUD, intros, results, pause and circuit styling. 132 tests pass; six desktop HUD-hidden captures, 24 phone-width aim captures/pixel checks and six Classic checks on 4181; real Schoolyard flick enters moving. Existing unattributed MutationObserver diagnostic remains; physical-device fps and full per-venue goal/replay stress outstanding. See `docs/ink-impact-art-direction.md`. No deployment or peer edits. |
| Guard zero-size viewport rendering | Codex | Commit containing this note | Reject zero/non-finite viewport dimensions before resizing renderer/post targets; suspend frame updates and rendering while collapsed, resume with zero elapsed delta. Three regression tests cover invalid dimensions, initially collapsed loop and paused recovery. Full suite: 132 pass, no skips; diff check clean. Mocked render-loop verification only; physical rotation/browser framebuffer verification remains. No deployment or peer edits. |
| Ink and Impact Roadside first visual slice | Codex | Commit containing this note | Original procedural cap poster, featured Roadside entry, scoped ink contours/light bands, service booths/kerbs, graphic HUD/results and bounded impact strokes. 129 tests pass. Desktop before/after and 320/375/390/430 iframe captures; real two-flick 1-0 win and three-star result. Art review, physical-phone performance and replay stress remain; known unattributed MutationObserver diagnostic recurred. See `docs/ink-impact-art-direction.md`. No deployment, no peer edits. |
| Add `.gitignore` for local tool debris | Claude Code | `478d936` | New `.gitignore` covering `.DS_Store` and `.playwright-cli/`: the repo had none, and the untracked debris in Codex's clone was blocking `collaboration-workflow.mjs sync` (it requires a clean tree before the fast-forward). Codex still has to remove or move its three existing untracked files once; after the sync the ignore rules keep them out. |
| Street Legends: Lights Out Final coin-stack chain + padlocked goals (3 acts); all six venues built | Claude Code | `2431cda` | Both goals padlocked (static bar segment). Three coin stacks per side, lit in beam order by the ball or your own cap on your flick; three lit = LIGHTS ON, padlock drops; the scorer's bulb goes out after every goal. Static `target` bodies record per-flick strikes in the touch bookkeeping (AI rehearsals see them). 127 tests pass. Browser: Act 3 chain lit with real flicks while Magic lit his own stack, goal through the open lock, LIGHTS OUT relocks next turn, classic Lights Out untouched, console clean. Balance: A1 62%; A2 37-10-53; A3 29-4-67 (easy Magic; medium was 6-44-50). Open: Act 2–3 draws, LIGHTS ON rare for the bot, human and phone playtest. |
| Street Legends: Veranda Derby clay-pot maze, no-straight-goals rule (3 acts) | Claude Code | `401c63a` | Three pots per half (r 0.13, big one in front of the keeper). A goal counts only if the ball bounced off a pot or the rail on that flick (`goalRequiresTouchOf`, real impacts only); straight goals waved off, ball thrown back out of the net. Goal detection moved to `goal-line-crossing-detection.js`. 121 tests pass. Browser: straight goal waved off, OFF THE POT goal → replay → results, classic Veranda untouched, console clean. Balance: A1 55% aware / 40% blind; A2 40-20-40; A3 40-13-47 (easy Yaw). Pot-only rule gave ~20% A1 across three layouts, so the rail counts too. Open: A1 just under 60%, human and phone playtest. |
| Street Legends: Schoolyard ruler seesaw + Kiosk change dish (3 acts each) | Claude Code | `eef5135` | New static segment collider (`addStaticSegment`) + pure segment helpers. Ruler turns 45° per attacker turn, sweeping resting caps/ball aside (cap pinning froze it ~40% of turns, removed). Dish covers 100° of each goal mouth, one notch per attacker turn. Street Legends lists 12 acts. 115 tests pass. Browser: RULER BANK and OFF THE DISH goals → replay → results; classic venues untouched; console clean. Balance: Schoolyard A1 78% / A2 37-7-57 / A3 46-12-42; Kiosk A1 65% / A2 30-40% wins / A3 25-21-54. Open: RULER BANK rare for the bot; Act 2–3 draws; human and phone playtest. |
| Street Legends: Harmattan Haze departing-lorry goals (3 acts) + mechanic registry | Claude Code | `20a7be1` | Goals ride toy lorries along the end line, one of 5 stops per attacker turn, ghost goal at the next stop. `physics.goalCenters` moves goal mouths (classic venues stay 0). Street Legends now lists all built acts. 104 tests pass. Browser: lorry moves and input waits, mesh and physics agree, CAUGHT THE LORRY → replay → results → Next Act, classic Harmattan unchanged, console clean. Balance: Act 1 82% (aware) vs 60% (blind); Act 2 30/27/43 W/L/D; Act 3 25/21/54. Open: Act 3 draws, human and phone playtest. |
| Street Legends vertical slice: Roadside toll gates (3 acts), Flick Vector redesign, chickens removed | Claude Code | `9bc233d` | New mode from the title; Classic and 2-Player untouched. Mechanic, AI and balance history: `docs/street-legends-design-matrix.md`, `plans/reports/playtest-260922-street-legends-roadside-toll-gates.md`. 98 tests pass. Browser: every act reaches play, a toll goal → hero label → replay → results → Next Act, classic Roadside has no mechanic, console clean. Open: Act 2/3 draw rates for the bot proxy (53–62%), human and phone playtest, sustained phone fps. Next venue order: `plans/260922-1136-street-legends-roadside-toll-gates/plan.md`. |
| Review + integrate broadcast, venue identity and Flick Vector range (`ca5d192`…`144fce4`) | Claude Code | `144fce4` (fast-forward) | Read the full range (88 files). Reran suite on r160: 90 pass, 0 fail, 0 skip; syntax clean, no src file >200 lines. On 4182/4180: all six venue previews and openings reach play with 0 GL errors and a clean console; real flick → moving → AI turn → goal → 3-star result; locked venue refuses entry; menu overlay covers the preview table; portrait 375×812 aim points up-field, camera locks while aiming and unlocks on release/cancel with no flick spent. MutationObserver diagnostic: an instrumented `observe` recorded zero calls from game code across all venues. With no source URL it is most likely injected by Codex's browser automation (`.playwright-cli/` snapshots in its clone), and it never appears in the in-app browser. Desktop now frames wider (`LANDSCAPE_FIT` 2.2×1.9) by design. No deployment. |
| Venue identity and Flick Vector implementation | Codex | `5301752`, `cb5a228` | Six constructions, seeded surfaces/markings, layered surroundings/openings, four-part mobile-readable aim and camera lock. 90 tests pass; all six venues/openings and 320/375/390/430 aim captured on 4181. Independent reviewer hit usage limit; physical-device checks and unattributed browser MutationObserver diagnostic remain. See `docs/venue-flick-vector-verification.md`. Claude clone unchanged; no deployment. |
| Collaboration workflow | Codex | Commit containing this note | Live cross-clone claim/dirty-file checks; owner-only claim records; clean, fast-forward-only sync; exact-commit handoff. 10 workflow tests and full 74-test suite pass, no skips. Claude clone unchanged. Claude must fetch and cherry-pick this workflow-only commit to adopt it; game redesign WIP excluded. No new browser verification needed for CLI-only changes. |
| Portrait framing (turn the view a quarter on tall screens) | Claude Code | `4eb8791` | Camera director now swings 90° below 0.95 aspect: pitch runs up the screen, you attack upward, table ~54% bigger at 390×844. Landscape identical (fit 4.04). Verified flick direction, goal push-in, intro crane, no console errors. |
| Mobile touch pass | Codex (built) + Claude Code (verified, merged) | `e982a1b` | Merged to `main`. Verified in browser at 375×812, 320×640, 640×360 and 844×390: 18px-miss touch grab works, pointercancel/blur abort without spending a flick, second finger ignored, mouse still flicks, right button ignored, HUD fits with no overflow, 48px controls, pause clear of sound. No console errors. Physical-device feel still unassessed. |
| Deployment documentation handoff (documentation only) | Codex docs | Commit containing this note | Claimed and completed deployment board entry, handoff report, and shared `docs/journals/` entry. Checked supplied evidence and documentation diff; public delivery remains blocked above. No implementation changes. |
| Realistic venue environments matched to pitch names | Claude Code | `1820560` | Ground, props, walls, fowl, shade, dust, night spill light, DOF sharp zone. Verified all 6 venues in browser, no errors. |
| Campaign, AI opponent, menus, game feel | Claude Code | `171cb18` | Full game loop verified; review fixes applied. |
