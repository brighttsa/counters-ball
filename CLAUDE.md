@AGENTS.md

# Claude Code notes

Everything in `AGENTS.md` applies. Codex works in its own clone at `/Users/bskt/Documents/ChatGPT/Counters ball`
on `codex/*` branches; `git fetch codex` then merge `codex/<branch>` into `main` after reviewing the
handoff note on `plans/agent-handoff-board.md`.
